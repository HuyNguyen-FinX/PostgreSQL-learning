-- =============================================================================
--  Nạp dataset nhỏ vào database `lab`.
-- =============================================================================
--  `lab_big` KHÔNG được nạp tự động ở đây: nó mất vài chục giây tới vài phút và
--  không phải lúc nào cũng cần. Lệnh nạp `lab_big` nằm ở 00-moi-truong/lab.md.
-- =============================================================================

\connect lab

\i /sql/02-seed.sql
